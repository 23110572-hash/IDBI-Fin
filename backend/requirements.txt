# --- API framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9        # file uploads (electricity bill)
httpx==0.27.0
websockets==12.0

# --- auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- persistence ---
sqlalchemy==2.0.30
psycopg2-binary==2.9.9         # Postgres driver (docker-compose); SQLite used as local fallback
redis==5.0.7
celery==5.4.0

# --- data quality / cloud ---
great-expectations==0.18.15
boto3==1.34.131                # Textract / S3 / Cognito (stubbed locally behind interfaces)

# --- ML package (installed editable in docker/local): provides the scorer + feature pipeline ---
# pip install -e ../ml   (brings numpy/pandas/scikit-learn/xgboost/shap)

# --- dev / quality ---
pytest==8.2.2
pytest-asyncio==0.23.7
ruff==0.4.9
