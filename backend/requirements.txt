# --- API framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9        # file uploads (electricity bill)
httpx==0.27.0                  # async HTTP client + FastAPI TestClient
websockets==12.0               # live alert feed (WebSocket)

# --- auth ---
python-jose[cryptography]==3.3.0   # HS256 JWT encode/decode

# --- persistence ---
sqlalchemy==2.0.30
psycopg2-binary==2.9.9         # Neon Postgres driver (prod); SQLite used as zero-setup fallback
celery==5.4.0                  # background + Mode-B jobs (runs inline via in-memory broker)

# --- ML package: provides the runtime scorer + feature pipeline ---
# pip install -e ../ml   (brings numpy/pandas/scikit-learn/xgboost/shap)

# --- dev / quality ---
pytest==8.2.2
pytest-asyncio==0.23.7
ruff==0.4.9
